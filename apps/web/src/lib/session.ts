/**
 * Session utilities placeholder.
 *
 * Implement your session management here — e.g., cookie-based sessions
 * using SESSION_SECRET from env vars, JWT tokens, or an auth library.
 */

export type Session = {
	userId?: string;
};

export function createSession(): Session {
	return {};
}
