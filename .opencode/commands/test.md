---
description: Run a single test file
agent: build
---

Run the specified test file using Vitest:

```
bunx vitest run $ARGUMENTS
```

If the test fails, analyze the failure and suggest a fix. If no file is specified, ask which test to run.
